import '../globals.css'

export const metadata = {
  title: 'Criar conta',
  description: 'Tela de criar conta Iel-ce',
}

export default function RootLayout({ children }) {
  return (
    <html lang="pt-br">
      <body>{children}</body>
    </html>
  )
}